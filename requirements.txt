# CommissionIQ - Python Requirements

# Core Dependencies (specific versions for compatibility)
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<1.6.0

# Dashboard (use older stable version if issues occur)
streamlit>=1.28.0,<1.40.0
plotly>=5.18.0,<6.0.0

# Optional (for future enhancements)
# fastapi>=0.104.0  # For production API
# uvicorn>=0.24.0   # ASGI server
# xgboost>=2.0.0    # Alternative model
# shap>=0.43.0      # Model explainability

# Note: If you encounter compatibility issues with numpy/streamlit,
# try installing specific versions:
# pip install numpy==1.26.4 streamlit==1.28.0

